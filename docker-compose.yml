services:
  postgress:
    image: postgres:17
    container_name: postgres-pcbuilder
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: pcbuilder
    ports:
      - "5429:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: